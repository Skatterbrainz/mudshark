# mudshark

mudshark is a package of Linux command line tools written in Rust to provide a consistent naming pattern of verb-noun, similar to PowerShell, but relying on base-level commands like dmidecode, lsblk, apt, flatpak. It also provides a consistent output to either table or JSON, as well as JSON compressed.


