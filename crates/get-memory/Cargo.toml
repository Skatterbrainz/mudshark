[package]
name = "get-memory"
version = "0.1.0"
edition = "2021"
description = "Report system memory and swap usage as a table or JSON."

[[bin]]
name = "get-memory"
path = "src/main.rs"

[dependencies]
mudshark-core = { path = "../mudshark-core" }
