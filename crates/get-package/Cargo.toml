[package]
name = "get-package"
version = "0.1.0"
edition = "2021"
description = "Report installed packages (dpkg): name, version, architecture, status."

[[bin]]
name = "get-package"
path = "src/main.rs"

[dependencies]
mudshark-core = { path = "../mudshark-core" }
